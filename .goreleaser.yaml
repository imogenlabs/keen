version: 2
project_name: atlas-cli
changelog:
  disable: true
builds:
  - id: atlas-cli
    main: ./cmd/jira-pp-cli-pp-cli
    binary: atlas-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X jira-pp-cli-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_arm64
      - linux_amd64
      - linux_arm64
  - id: atlas-mcp
    main: ./cmd/jira-pp-cli-pp-mcp
    binary: atlas-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_arm64
      - linux_amd64
      - linux_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: checksums.txt
brews:
  - name: atlas-cli
    repository:
      owner: smashingtags
      name: homebrew-tap
    homepage: "https://github.com/smashingtags/jira-pp-cli"
    description: "Complete Atlassian platform CLI with MCP server and SQLite sync"
    install: |
      bin.install "atlas-cli"
      bin.install "atlas-mcp"
