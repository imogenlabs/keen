version: 2
project_name: keen
changelog:
  disable: true
builds:
  - id: keen
    main: ./cmd/keen
    binary: keen
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X keen/internal/cli.version={{ .Version }}
    targets:
      - darwin_arm64
      - linux_amd64
      - linux_arm64
  - id: keen-mcp
    main: ./cmd/keen-mcp
    binary: keen-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_arm64
      - linux_amd64
      - linux_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: checksums.txt
brews:
  - name: keen
    repository:
      owner: smashingtags
      name: homebrew-tap
    homepage: "https://github.com/imogenlabs/keen"
    description: "Complete Atlassian platform CLI with MCP server and SQLite sync"
    install: |
      bin.install "keen"
      bin.install "keen-mcp"
